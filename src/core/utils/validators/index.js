const emailValidator = require('./emailValidator');
const hasValueValidator = require('./hasValueValidator');
const passwordValidator = require('./passwordValidator');

module.exports = {
    emailValidator,
    hasValueValidator,
    passwordValidator
}